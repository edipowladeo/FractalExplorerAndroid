package com.exploradordefractais

import android.os.Build
import androidx.annotation.RequiresApi

class FractalResources {
    @RequiresApi(Build.VERSION_CODES.N)
    val janela = FractalJanela(this)
}